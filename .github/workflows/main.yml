name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: peaceiris/actions-pipenv@3.7
    - name: Run Tests
      run: |
        pipenv sync --dev
        ls
        make install
        make tests
